import { combineReducers } from 'react-redux'



const initialState = {
    products:[]
}

function addProduct(state = initialState, action = {}){
    switch(action.type){
    }
}

function removeProduct(state = initialState, action = {}){

}

export default {
    addProduct,
    removeProduct
}